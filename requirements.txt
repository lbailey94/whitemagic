# WhiteMagic Local Development Dependencies
# Includes everything: core + embeddings + dev tools

# Core WhiteMagic package with local embeddings support
# [embeddings] extra includes torch + sentence-transformers for privacy-first local embeddings
-e .[embeddings,dev]

# Note: For Railway/production deployment, use requirements-railway.txt instead
# That file excludes heavy ML dependencies and uses OpenAI embeddings
