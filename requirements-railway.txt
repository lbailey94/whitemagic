# WhiteMagic Railway Deployment Dependencies (Optimized)
# Excludes heavy ML dependencies (torch, sentence-transformers)
# Uses OpenAI embeddings for semantic search instead of local models

# Core WhiteMagic package (local install)
-e .

# Database
sqlalchemy>=2.0.0
asyncpg>=0.29.0
aiosqlite>=0.19.0
alembic>=1.13.0

# API Framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Rate Limiting
redis>=5.0.1

# HTTP Client
httpx>=0.25.1

# AI/Embeddings (OpenAI only - lightweight)
openai>=1.3.0
# NOTE: sentence-transformers and torch excluded for Railway
# Local embeddings can be used in development, but Railway uses OpenAI

# Core Dependencies
pydantic>=2.5.0
pydantic-settings>=2.1.0
numpy>=1.24.0
rich>=13.0.0

# Logging
python-json-logger>=2.0.7

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
