# WhiteMagic Environment Variables
# Copy this file to .env and customize for your setup.

# --- State Root ---
# Where WhiteMagic stores its runtime data (memory DB, karma ledger, vault, etc.)
# Default: ~/.whitemagic
# WM_STATE_ROOT=~/.whitemagic

# --- MCP Server ---
# Enable PRAT mode (28 Gana meta-tools instead of 311 individual tools)
# WM_MCP_PRAT=1

# Suppress startup log noise
# WM_SILENT_INIT=1

# Enable debug logging (verbose — shows registry loading, dispatch details)
# WM_DEBUG=1

# CORS allowed origins (comma-separated). Default: localhost only.
# Set to * ONLY in fully trusted/isolated environments.
# WM_MCP_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# --- Gratitude Economy (XRPL) ---
# Your public XRP receive address for tip collection.
# Get one from XUMM wallet, a hardware wallet, or an exchange.
# NO private keys — WhiteMagic is receive-only by design.
# WM_XRP_ADDRESS=rYourXRPAddressHere

# Optional: destination tag for exchange deposits
# WM_XRP_DEST_TAG=

# --- Local AI (llama.cpp) ---
# llama-server URL for local LLM inference
# WM_LLAMA_HOST=localhost
# WM_LLAMA_PORT=8080

# --- Redis (optional, for multi-agent coordination) ---
# REDIS_URL=redis://localhost:6379

# --- Vault Encryption ---
# Passphrase for the encrypted secrets vault
# If not set, vault will prompt or use OS keychain
# WM_DB_PASSPHRASE=

# --- x402 Micropayments (Base L2) ---
# Your Base L2 wallet address for receiving x402 micropayments
# Get from Coinbase app: Receive → USDC → Base network
# WM_X402_WALLET_ADDRESS=0xYourBaseL2AddressHere

# --- Web Research ---
# Brave Search API key (free tier: 2000 queries/month)
# Get one at https://brave.com/search/api/
# If not set, falls back to DuckDuckGo HTML scraping
# BRAVE_SEARCH_API_KEY=

# --- External APIs (store in vault instead of env for production) ---
# OPENAI_API_KEY=
# BASESCAN_API_KEY=
