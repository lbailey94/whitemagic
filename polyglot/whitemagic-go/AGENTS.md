# Whitemagic Go

## Project Context

Go microservices for the whitemagic distributed system.

## Conventions

- **Language**: Go 1.21+
- **Style**: gofmt, go vet
- **Error handling**: Always handle errors, never ignore with `_ = `

## Structure

- `pkg/` — Library packages
- `cmd/` — Main applications
