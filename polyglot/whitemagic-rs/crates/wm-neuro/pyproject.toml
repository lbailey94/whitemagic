[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "wm-neuro"
version = "23.3.1"
description = "PyO3 bindings for WhiteMagic neuro-cognitive hot-path modules"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
